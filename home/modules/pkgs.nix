{ ... }:

{
  #############################################################
  ## Пакеты
  #############################################################
  # Пакеты только для пользователя ставятся сюда; системные — в
  # environment.systemPackages в hosts/modules/pkgs.nix.
  home.packages = [];
}
